TFWS v2 — Beta Release (v2.0.0-beta.2)

Includes:
- Schemas + validated examples (trust-state, incident, key-history)
- Full documentation set (Getting Started, Schemas map, Governance, Compliance, Use Cases, Architecture)
- tfws2 CLI tooling (validate/hashwalk/minisign verify/key epoch/rollback simulation)
- Reference Trust API (FastAPI) returning schema-valid trust-state + well-known probes + inventory minisign verification
- Agent Decision Playground (local + HTTP)

Status: Beta. Schemas stable; tooling/API may evolve.
